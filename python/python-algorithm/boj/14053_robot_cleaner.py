def q1(arr, i, j):
    if arr[i][j] == 0:
        arr[i][j] = 2
    return arr[i][j]

def q2(arr, i, j):
    di = [-1, 1, 0, 0] # 상하좌우
    dj = [0, 0, -1, 1]

    ni = i + di[]
    nj = j + dj[]

    if arr[ni][nj]
    return

def q3(arr, i, j):
    return

